
import type { TestSeriesType, QuestionType } from "./types";

export const APP_NAME = "Vidyaheist";

// Mock data is no longer needed as we are migrating to a database.
export const MOCK_TEST_SERIES: TestSeriesType[] = [];

export const MOCK_QUESTIONS: QuestionType[] = [];
